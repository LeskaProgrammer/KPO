# Отчет по КПО

**Тема:** Асинхронное межсервисное взаимодействие (Интернет-магазин "Гоzон")

**Выполнил:** Симонов Алексей

**Группа:** БПИ248

---

## 1. Технологический стек

* **Бэкенд:** Python 3.11, FastAPI, PostgreSQL (SQLAlchemy + asyncpg). У каждого сервиса своя БД.
* **Инфраструктура:** RabbitMQ (очереди), Nginx (шлюз), Docker Compose.
* **Фронтенд:** Простой SPA на чистом JS + HTML5.
* **Протоколы:** HTTP (REST), AMQP (RabbitMQ), WebSockets.

## 2. Архитектура и решения

Система состоит из микросервисов **Order** и **Payment**. Главная цель — надежность данных при сбоях сети.

### Реализованные паттерны

#### 1. Transactional Outbox (Гарантия отправки)
Вместо прямой отправки в RabbitMQ, мы сначала сохраняем сообщение в БД (таблица `outbox`) в одной транзакции с созданием заказа. Фоновый процесс затем отправляет эти сообщения. Это гарантирует, что событие не потеряется, даже если упадет сеть или брокер.

#### 2. Transactional Inbox + Идемпотентность (Защита от дублей)
RabbitMQ может прислать одно сообщение дважды. Чтобы не списать деньги повторно, сервис платежей проверяет таблицу `inbox`. Если уникальный `message_id` там уже есть, обработка пропускается.

#### 3. Атомарные операции (Защита от гонки)
Изменение баланса происходит прямым SQL-запросом (`balance = balance - amount`), а не вычислением в коде Python. Это предотвращает уход в минус при одновременных покупках.

#### 4. WebSockets (Уведомления)
Как только оплата проходит, сервер мгновенно отправляет пуш-уведомление на фронтенд. Статус заказа меняется на "Оплачено" без перезагрузки страницы.

## 3. Структура проекта

```text
Project_Root/
├── docker-compose.yml       # Оркестрация всех контейнеров системы
├── init_db.sql              # Инициализация БД (схемы таблиц, Inbox/Outbox)
├── nginx.conf               # Конфигурация API Gateway (маршрутизация)
├── README.md                # Отчет и документация по запуску
├── swagger_json             # Спецификация API
├── test_script.py           # Скрипт для тестирования и проверки
│
├── frontend/                # Директория с файлами фронтенда
│   └── index.html           # приложение с поддержкой WebSockets
│
├── order_service/           # Микросервис управления заказами
│   ├── Dockerfile           # Сборка образа для сервиса заказов
│   ├── main.py              # Логика сервиса (FastAPI + Transactional Outbox)
│   └── requirements.txt     # Зависимости (FastAPI, uvicorn, aio-pika)
│
└── payment_service/         # Микросервис платежной системы
    ├── Dockerfile           # Сборка образа для сервиса платежей
    ├── main.py              # Логика сервиса (Inbox, Idempotency, Atomic Updates)
    └── requirements.txt     # Зависимости
```

## 4. Чек-лист выполненных критериев

Работа выполнена в полном объеме, включая дополнительные задания на 10 баллов.

**Базовые требования (до 8 баллов):**
- [x] **Разделение на сервисы:** Четко выделены Order Service и Payment Service.
- [x] **Очереди сообщений:** Взаимодействие реализовано через RabbitMQ (Topic Exchange).
- [x] **Transactional Outbox:** Реализован в обоих сервисах (таблица outbox, фоновый воркер).
- [x] **Transactional Inbox:** Реализован в Payment Service (таблица inbox для дедупликации).
- [x] **Exactly-once семантика:** Обеспечена защита от повторного списания денег.
- [x] **Docker:** Написаны Dockerfile и docker-compose.yml.

**Дополнительные требования (до 10 баллов):**
- [x] **I. Фронтенд:** Реализован отдельный сервис (Nginx container) с веб-интерфейсом.
- [x] **II. WebSocket:** Реализовано реальное отслеживание статуса. Клиент получает мгновенное уведомление об успешной оплате или отмене заказа.

## 5. Инструкция по запуску


### Запуск системы:

Для копирования:

    docker-compose up --build

После долгой сборки появится сообщение `Application startup complete` в логах.

### Доступ к приложению:

Откройте браузер по адресу:
👉 [http://localhost](http://localhost)

### Сценарий проверки:

1.  Введите ID пользователя (например, `1`) и нажмите **"Подключиться к WS"** (индикатор должен стать зеленым).
2.  Если счета нет, нажмите **"Создать счет"**.
3.  Пополните баланс кнопкой **"Пополнить"**.
4.  В блоке "Покупка" нажмите **"Купить"**.

**Результат:** В списке появится новый заказ (`NEW`), через секунду всплывет уведомление, и статус сменится на `FINISHED` (зеленый) или `CANCELLED` (красный, если денег мало).


## 6. Тестовый скрипт (test_script.py)

Для проверки отказоустойчивости и корректности работы транзакций написан тест.

**Сценарий тестирования:**

1.  **Setup & Balance:** Регистрация пользователя, пополнение счета и верификация баланса.
2.  **Недостаток средств:** Попытка покупки на сумму, превышающую баланс (ожидается статус `CANCELLED`).
3.  **Защита от Race Condition (Double Spending):**
    * Отправка **5 одновременных запросов** на списание всей суммы баланса.
    * **Критерий успеха:** Ровно 1 транзакция успешна, 4 отклонены. Итоговый баланс строго `0.0` (счет не уходит в минус).
    
**Запуск теста:**
Нужно убедиться, что контейнеры запущены, затем выполнить:
```bash
python test_script.py
```
